{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://localhost/latest/schema/ssn/Sensor.json",
  "title": "Sensor",
  "allOf": [
    {
      "$comment": "Property validation is separated, to allow pointers that validate properties where present, but do not enforce the required properties array",
      "properties": {
        "@type": {
          "oneOf": [
            {
              "type": "string",
              "pattern": "^(sosa:)?Sensor$"
            },
            {
              "type": "array",
              "contains": {
                "type": "string",
                "pattern": "^(sosa:)?Sensor$"
              }
            }
          ]
        }
      }
    },
    {
      "type": "object",
      "required": [
        "@type"
      ],
      "definitions": {
        "sensorProperties": {
          "$comment": "Property validation is separated, to allow pointers that validate properties where present, but do not enforce the required properties array",
          "properties": {
            "@type": {
              "oneOf": [
                {
                  "type": "string",
                  "pattern": "^(sosa:)?Sensor$"
                },
                {
                  "type": "array",
                  "contains": {
                    "type": "string",
                    "pattern": "^(sosa:)?Sensor$"
                  }
                }
              ]
            }
          }
        }
      }
    }
  ]
}